# カードイレブン (Card Eleven) — 仕様書 SPEC

> **この文書がプロジェクトの正本(Single Source of Truth)です。**
> 実装(`カードイレブン.html`)とこのSPECが食い違った場合、原則SPECを正とし、
> 仕様変更は「①SPECを更新 → ②実装 → ③SPECと一致を検証」の順で行う。
> 数値(確率・係数・閾値)は意図を持った決定事項なので、変更時は必ずここを更新する。

- **バージョン**: SPEC v1.9 / セーブデータ schema v9
- **形式**: 単一HTMLファイル(`カードイレブン.html`)。外部依存なし、オフライン動作。
- **想定プレイ**: スマートフォン縦持ち・1人プレイ。1試合 約1〜2分。
- **コンセプト**: SEGA WCCF 風の、ドット絵コレクティブルカードサッカー。SFC(スーパーファミコン)レトロ調。

---

## 1. アーキテクチャ / 技術仕様

| 項目 | 仕様 |
|---|---|
| 構成 | 単一 `.html`。`<style>` + `<script>`(`"use strict"`)に全て内包 |
| 永続化 | `window.storage`(キー `"ci-save"`)に JSON 文字列で保存。get 3秒 / set 2.5秒のタイムアウト付き |
| 画像 | 頭部・ボディのスプライトатлась 2枚を WebP base64 データURIで埋め込み(`HEAD_IMG` / `BODY_IMG`) |
| 起動堅牢化 | 画像は `decode()` ではなく onload+4秒タイムアウト。失敗時は簡易シルエットで描画継続 |
| 診断 | 起動時、画面最上部に `BOOT 1/3〜3/3` の進捗バー。本体JSと独立したES5エラーハンドラで行番号付きエラーを表示。成功で3秒後に自動非表示 |
| フォント | `DotGothic16`(ドットフォント)。角丸全廃、RPGウィンドウ枠、CRTスキャンライン、ディザ背景 |
| アクセシビリティ | `prefers-reduced-motion` でアニメーション停止 |

### 不変条件(リグレッション禁止)
- **参照ID整合**: JS内の全 `getElementById("X")` に対し、HTMLに `id="X"` が必ず存在する。
- **ポジション整合**: 全カードで `subGroup(c.sub) === c.pos`(細分posは必ず大分類1つに属する)。FORMS各枠は細分posで定義し、大分類は `subGroup()` で導出する。
- **試合完走**: 全4スタイル(center/side/long/short)で試合が最後まで進行する。
- **ハング耐性**: ストレージ・画像が応答しなくても起動し、試合が完走する。
- マッチアップ計算は**比率ベース**。全選手を一律スケールしても勝率バランスは不変。

---

## 2. データモデル

### 2.1 セーブ状態 `S`(schema v8)
```
{ coins:300, coll:[], squad:{}, form:"4-4-2", cleared:0,
  tactic:"bal", v:9, legendPacks:0, league:null, nextId:<uid> }
```
- `coll`: 所持カード配列 / `squad`: {スロットindex → カードid} / `cleared`: 攻略済みステージ数
- `legendPacks`: 未開封レジェンドパック数 / `league`: リーグ戦の進行状態(後述)

### 2.2 カード
```
{ id, name, flag, pos, sub, rar, type, off, def, pow, tec, spd, sta, skill, look }
```
- `pos`: 大分類 `GK|DF|MF|FW`(スプライト・タイプ・スキルの基準) / `sub`: 細分ポジション(§3.4) / `rar`: `n|r|sr|l` / `type`: プレースタイル(§4)
- 6ステータス `off攻 def守 pow力 tec技 spd速 sta持`(各1〜20)
- `flag`: 国籍(国旗絵文字、16ヶ国)。国名は `NATIONS[flag]` で導出(§3.5)。
- `look`: 見た目(headIdx 0-31, bodyVar 0-3, 及び旧フォールバック描画用の skin/hair/kit 等)

### 2.3 マイグレーション
- 起動時 `S.v !== 9` なら `migrate()` 実行 → `save()`。
- **v8変換**: 旧1-9スケール(6ステ合計≤54)のカードを、レア度別目標合計へ比例リスケール(§3.1)。
  各能力の高低バランスは保持。coins/cleared/legendPacks 等は維持。
- **v9変換**: `sub` 未設定のカードに、`c.pos` の大分類に属する細分posを `SUBS_BY` からランダム付与(§3.4)。既存進捗は維持。

---

## 3. ステータス & レアリティ

### 3.1 レアリティと目標合計(1-20スケール)
| レア | 表記 | 6ステ合計(目標) | 1項目平均 | 生成基準 `RAR_TOTAL` |
|---|---|---|---|---|
| n  | ★      | 55  | 9.2  | 53 |
| r  | ★★     | 70  | 11.7 | 68 |
| sr | ★★★    | 90  | 15.0 | 88 |
| l  | LEGEND | 100 | 16.7 | 98 |

- `RAR_TOTAL` は丸め上振れを見込んだ内部目標(実測が目標値≒55/70/90/100に乗る)。
- **生成方式**: 6値に分散を持たせ、合計を `RAR_TOTAL` へ正規化し、1〜20にクランプ再配分。
  → 同レアでも個体差が出るが、合計は安定。
- `scaleTo(c, target)`: 既存6ステ合計を target へ比例スケール(1-20クランプ)。対戦相手生成と v8 移行で使用。

### 3.2 ポジション補正(生成後に加算)
| pos | 補正 |
|---|---|
| GK | def +3, off −3 |
| DF | def +2, pow +1, off −2 |
| MF | tec +2, sta +1 |
| FW | off +2, spd +1, def −2 |

### 3.3 ガチャ確率(プレイヤーパック・🪙100)
| 排出 | 確率 |
|---|---|
| sr | 5% |
| r  | 25% |
| n  | 70% |
- **LEGEND は通常ガチャから排出されない**(レジェンドパック専用)。

### 3.4 細分ポジションと2段階適正(pen)
大分類(`pos`: GK/DF/MF/FW)に加え、各カードは細分ポジション `sub` を1つ持つ。

| 大分類 | 細分pos(`SUBS_BY`) |
|---|---|
| FW | CF, ST, LWG, RWG |
| MF | OMF, CMF, DMF, LMF, RMF |
| DF | LSB, CB, RSB |
| GK | GK |

- 各細分posの大分類は `SUBGROUP[sub]` で導出(`subGroup(c.sub)===c.pos` が常に成立)。
- **大分類内の近接クラスタ `SUBNEAR`**(同クラスタ内=近接、別クラスタ=非近接):
  - FW: {CF, ST} / {LWG, RWG}
  - MF: {OMF, CMF, DMF} / {LMF, RMF}
  - DF: {LSB, RSB} / {CB}
  - GK: {GK}

**2段階適正 `posFit(cardSub, slotSub)` → pen 係数**(`POSFIT`):

| 段階 | 条件 | pen |
|---|---|---|
| 完全一致 | 細分posまで一致 | **1.00** (`exact`) |
| 同分類・近接 | 大分類一致 & 同クラスタ | **0.92** (`near`) |
| 同分類・非近接 | 大分類一致 & 別クラスタ | **0.85** (`far`) |
| 大分類違い | 大分類が異なる(例 MF→FW枠) | **0.72** (`group`) |

判定順は「①大分類の一致/不一致 → ②同分類内の細分(完全一致/近接/非近接)」の2段階。pen は試合エンジンの有効値計算(§6.1)に直接乗る。

- FORMS各枠は細分posで定義(`[sub, x, y]`)。大分類は `subGroup(sub)` で導出。座標は下記の標準ポジショングリッドに従う。
- **標準ポジショングリッド**(上=攻撃/下=自陣、x:左0〜右100、y:上0〜下100):
  - 最前列 CF / LWG / RWG ≈ y13–15、ST ≈ y28、OMF ≈ y38、MF段 LMF/CMF/RMF ≈ y49–51、DMF ≈ y60、DF段 LSB/CB/RSB ≈ y73–77、GK ≈ y90。
  - 中央列は CF→ST→OMF→(CMF)→DMF→CB→GK の順に縦に並べ、CF と ST のような同列の混同を防ぐ。
  - 同一細分が複数枠あるとき(CB×2 等)は x を左右に振って重なりを回避。
- 編成画面の可視化:
  - 各枠の**上部に適正ポジション名(slothead)**を常時表示(色は大分類)。
  - フィールドを **FW(赤)/MF(緑)/DF(青)/GK(金)** の帯ゾーンで色分け表示(`.zones`、y境界 0/33/69/86%)。
  - 配置カードの適正は枠色＋マークで表示: **✓**=完全一致(緑枠) / **金枠+本来の細分名**=同分類の細分違い / **赤枠+本来の細分名**=大分類違い。
- ピッカー/おまかせ/交代候補のソートは `posFit` 降順 → 総合値降順。
- `makeCard(pos, rar, base, sub)` の第1引数は大分類だが、細分pos(例 "LSB")を渡しても大分類へ正規化し細分として採用する(FORMS各枠の `sl[0]` をそのまま渡せる)。

### 3.5 国籍とケミストリー
- 各カードは国旗 `flag`(16ヶ国)を持つ。国名は `NATIONS[flag]` で導出(`natName`)。明示的な国名フィールドは持たず `flag` が正なので**マイグレーション不要**。
- **図鑑の国籍フィルタ**(`collFilter` チップ): 所持国籍ごとに枚数を表示し、タップで絞り込み。「全て」も常設。在庫が無くなったフィルタは自動的に「全て」へ戻す。
- カードタップで `🇪🇸 スペイン代表・名前(sub)|【スキル】説明` をトースト表示。
- **同国籍ケミストリー**: 編成された11人のうち**最多同国籍人数 `mx`** に応じてチーム全体を微強化。
  - 係数 `chem = 1 + min(0.06, max(0, mx−2)×0.012)`(3人=+1.2% / 5人=+3.6% / 7人以上=+6%上限)。
  - `recalcAuras` で算出し、`eff()`(全マッチアップ・シュート・GK守備の単一集約点)に乗算。
  - **両チームに対称適用**(相手も国籍がランダムに付くため公平)。編成画面に現在のケミストリー、試合開始時に発動実況を表示。

---

## 4. プレースタイル(タイプ)

全選手がポジション別に3〜4種のタイプを持つ。タイプは「活動場所・積極性・ポゼッション・イベント選出率・オフザボールの動き」に影響する。

### 4.1 パラメータ定義
| キー | 意味 |
|---|---|
| `adv` | 前後オフセット(+で前線寄り) |
| `wide` | サイドへの張り出し |
| `roam` | 徘徊量(ランダム移動の大きさ) |
| `chase` | ボールへの吸着度 |
| `poss` | 支配率への貢献倍率 |
| `atk/tgt/pas/defSel` | 各イベントの選出重み倍率 |
| `run` | オフザボールの飛び出し頻度 |
| `wideSel` | サイドアタック起点になれる適性 |

### 4.2 タイプ一覧
**FW**: ストライカー(striker, 最前線/シュート関与大/poss低) / ポストプレイヤー(post, 低めで収める/tgt最大/poss高) / ドリブラー(dribbler, サイドに流れ大きく動く) / シャドー(shadow, ライン間で飛び出し)
**MF**: ゲームメーカー(maker, 中央低め/poss最高1.3) / ボックストゥボックス(b2b, 縦に往復/run最大) / ウインガー(winger, タッチライン際) / アンカー(anchor, DF前/守備選出)
**DF**: ストッパー(stopper, 前に出て潰す/defSel1.5) / カバー(cover, 最後尾) / 攻撃的SB(sb, 外を上下動)
**GK**: シュートストッパー(liner, ライン上) / スイーパー(sweeper, 高い位置/poss1.4) / コマンダー(commander, 中間)

> 実数値は実装の `TYPES` 定数を参照(本SPECでは設計意図を記載。数値変更時は両方更新)。

---

## 5. スキル

`fx` のキーが試合エンジンで参照される効果フラグ。

### 5.1 効果キー
`save`(GKセーブ) `duelD`(守備マッチアップ) `duelSpd/duelPow/duelTec`(各勝負) `shoot`(シュート) `mid`(個人支配率) `teamChance`(チーム創出) `teamDef`(チーム守備) `iron`(消耗なし) `clutch`(70分以降) `losing`(ビハインド時) `miracle`(失点無効・LEGEND)

### 5.2 通常スキル(R=1個 / SR=1個強力。N=なし)
- GK: R[セービング, 飛び出し] / SR[神の手, 守護神]
- DF: R[ハードプレス, 鉄人] / SR[完全密着, 統率]
- MF: R[パスワーク, ダイナモ] / SR[司令塔, ファンタジスタ]
- FW: R[決定力, スピードスター] / SR[怪物, 勝負師]

### 5.3 レジェンド専用スキル(`LSKILLS`、LEGENDのみ)
- GK: **奇跡の手**(1試合1回 失点無効化 + MIRACLE!!カットイン) / 絶対領域(save 1.7)
- DF: 鉄壁の壁(duelD 1.7) / 守護王(teamDef 1.3)
- MF: 王の視野(teamChance 1.4) / マエストロ(mid 1.6 + duelTec 1.35)
- FW: 伝説の一撃(shoot 1.5) / 覇王(duelSpd/Pow/Tec 各1.3)

---

## 6. 試合エンジン

### 6.1 基本式
- **有効値** `eff(p,k) = stat × pen × fatigue × situ`
  - `pen`: 細分ポジション適正 `posFit`(§3.4)。完全一致=1.0 / 同分類近接=0.92 / 同分類非近接=0.85 / 大分類違い=0.72
  - `fatigue = 1 − (経過分/90)×0.35×(1 − (sta−1)/19)`(sta20で減衰なし。iron持ちは常に1)
  - `situ`: clutch(70分〜)、losing(ビハインド時)を乗算
- **ランダム** `rr() = 0.6 + random×0.8`(範囲0.6〜1.4)。各スコアに乗る。
- **支配率** `midPower`: 中盤の `tec×0.45 + spd×0.3 + sta×0.25` を選手ごとに加重(MF=1, 他=0.32)× `mid` × `poss`。tactic(atk1.05/def0.92)・style(short1.06/long0.94)補正。
  - 攻撃権は `mh/(mh+ma)` の確率でホーム。

### 6.2 閾値 `TH`(攻撃側スコア > 守備側スコア × 閾値 で成功)
| キー | 値 | 用途 |
|---|---|---|
| duel | 1.1 | 中央1対1 |
| gk | 0.88 | シュート vs GK |
| side | 0.9 | サイド突破 |
| cross | 0.78 | クロスの競り合い |
| longPass | 0.95 | ロングフィード成立 |
| longRace | 1.08 | 裏抜けの駆けっこ |
| chain | 0.92 | ショートパス連携成立 |
| shortBonus | 1.12 | ショート成功後の勝負ボーナス |

### 6.3 攻撃スタイル別シーケンス
- 🎯 **center**: ペナルティエリア手前で得意勝負(spd/pow/tec を能力に応じ選択)→ tryShot
- 🏃 **side**: ウイングがタッチライン際を駆け上がる → 突破ならクロス → ターゲットがヘディング勝負 → tryShot(header)
- 🚀 **long**: 後方の起点 → 最前線へロブ → 受け手とDFが落下点で競走 → 裏抜けで GKと1対1 → tryShot
- 🔄 **short**: MF→MFのワンツー → 連携成立で中央勝負(shortBonusボーナス付き)
- **ロングカウンター**: 守備側が long スタイルだと22%で攻撃権を奪取。

### 6.4 シュート判定 `tryShot`
- `sBase` = 通常 `off×0.7+pow×0.3` / ヘディング `off×0.45+pow×0.55`
- `sSc = sBase × shoot × rr()` 、`gSc = GK.def × save × teamDef × rr()`
- `sSc > gSc × 0.88` で得点。**奇跡の手**保持GKは1試合1回これを無効化。

### 6.5 演出(縦ピッチ)
- フィールドは縦長(3:4)。自陣下・敵陣上。内部座標(攻撃軸x, 横y)を `toScreen(x,y)={sx:6+y*0.88, sy:97−x*0.94}` で画面に回転変換。
- ボール・選手が実際に移動(steps補間でカクカク=SFC感)。陣形ブロックがボールに追従して伸縮(最終ライン ball−34%、最前線 ball+16%)。コンパクトネス12%。積極性の高い選手2人が毎ティック飛び出す。
- カットイン: VS(SR/L同士または18%)、シュート!!、GOAL!!!(金レイ+画面シェイク)、SAVE!!、MIRACLE!!。
- チーム識別: 足元の発光リング(自軍=青H、敵=赤A)。ユニフォーム色はアトラスに焼き込み済み。

---

## 7. ゲームモード

### 7.1 ステージ攻略
- 8クラブ(`CLUBS`)を Lv1→Lv8 で順次解放。`cleared` で進捗管理。
- 各クラブは `[名前, 強さLv, 得意フォーメーション]` を持ち、相手は**その陣形で実際に布陣する**(座標・細分pos・KEYPOSもその陣形のもの)。`oppTeam(lv, form)`。
  - 1選手平均 `avg = 6.6 + lv×1.0`(Lv1≈7.6 → Lv8≈14.6)。Lv8 のみエースFW1名が LEGEND(その陣形のFW枠からランダム選出)。
- **フォーメーション相性(`FORM_COUNTER`)**: 守備側の陣形に対し、攻撃側スタイルの有利(best)/不利(worst)を定義。攻撃側の主判定(`tfA`経由・全スタイル共通)に best=×`COUNTER_BONUS`(1.15) / worst=×`COUNTER_PENALTY`(0.88) を乗算。**両チームの攻撃に対称適用**(form未設定のクローン対戦は等倍=バランス不変)。
  - best/worst は試合エンジンの**実測**(同強度クローンの陣形別スタイル得点)に一致させ、ヒント表示と実挙動が食い違わないようにしている。値は「自然な傾向の増幅」であり勝率を支配しない(SPECの"スタイルはpower pickでなく戦術選択"方針を維持)。主効果は得点・チャンス創出という体感と、接戦帯での数%の優位。
  - 相手フォーメーションと推奨スタイルは**ステージ選択画面で事前開示**+キックオフ時にヒント表示。プレイヤーは相手に応じて編成・スタイルを変える動機を持つ。
- **勝利報酬** `100 + lv×40` コイン / 引分 50 / 敗北 30。
- 想定勝率カーブ(`progtest2`・centerスタイル固定での実測。相手陣形多様化後): N→Lv1(4-4-2) 78% / N→Lv2(5-3-2) 43% / R→Lv3(4-3-1-2) 93% / R→Lv4(4-2-3-1) 82% / SR→Lv5(4-3-3) 100% / SR→Lv6(5-3-2) 95% / SR→Lv7(4-3-1-2) 37% / L→Lv8(4-2-3-1) 93%。
  - **Lv2・Lv7の谷はcenter固定でゴリ押した場合の値**。相手の推奨スタイル(Lv2=short, Lv7=long)に変えれば改善する=「単一戦略の抑止／相手に応じた対応」が機能している証跡。旧基準(全4-4-2): N→Lv1 73%/Lv2 63%、R→Lv3 88%/Lv4 48% 等。

### 7.2 リーグ戦
- 自チーム+8クラブ=全9チームの総当たり(円卓法・BYE枠で偶数化 → 9節・各チーム8試合・ユニーク対戦36)。
- 自分の試合はフル演出、他カードは `simCpu`(クラブLvベースのポアソン風)で即時シミュレート。
- 順位表: 勝点(勝3/分1/負0)→ 得失点差 → 得点 でソート。自チーム行をハイライト。
- 報酬: 優勝=🪙500+チャンピオンパック×1 / 2-3位=🪙250 / 4位以下=🪙100。何度でも再挑戦可。

### 7.3 交代
- 1試合3回まで。投入選手は投入時刻からのスタミナ(fresh)。ポジション不一致は pen 0.72。

### 7.4 ガチャ / パック開封UI
- パックは `PACKS` 配列で**データ駆動**定義(`id/name/emoji/color/cost/desc/can()/pay()/owned()/get()`)。新パック追加は配列に1要素足すだけで一覧・開封演出に反映(将来のリーグ/モード報酬パックに対応)。
- ガチャ画面はタイル一覧(`renderGacha`)。各タイルにコスト/所持数/排出内容を表示し、入手不可はグレーアウト。
- 開封演出はフルスクリーン `#packOverlay`: ①パックがフロートイン(`floatUp`+`bob`)→ ②タップ(または約1.7秒で自動)で破裂(`burst`+フラッシュ)→ ③カードが飛び出す(`flyIn`・段差)→ 最高レア度に応じた結果バナー(SR=✨ / LEGEND=🌈 発光)+「とじる」。
- **ロジックとUIを分離**: 純抽選 `drawPack(id)`(pay+get+所持追加、演出なし)を `openPackById`(演出付き)と検証ハーネスが共用。
- レジェンドパックはコイン購入不可・`owned()` で所持数表示・試合後ドロップのみ(§8)。`prefers-reduced-motion` で演出停止。
- **チャンピオンパック**(`id:"champion"`, 🏅): リーグ優勝報酬(`S.championPacks`)。1パック=**5枚**(高排出4枚 + SR以上1枚確定、確定枠の18%でLEGEND)。`championDraw()` で生成。コイン購入不可。実測内訳の目安: N≈12% R≈31% SR≈44% L≈11%。

### 7.5 ユーザーインターフェース(スマートフォン最適化)
- **試合画面レイアウト**: フィールド表示を `.fieldview max-height:200px`（従来62vh から95%削減）に縮小し、実況フィード・戦術ボタン・交代操作が全て画面内に収まるようコンパクト化。スマートフォンでのスクロール操作を排除。
- **モーダルダイアログ**: フォーメーション選択・選手交代・カードピッカーの3モーダルに**window-style☒ close ボタン**(位置:絶対)をシート左上に配置。`.sheet max-height:50vh`（従来70vh）制限+スクロール許可で、ボタンが常に見える範囲に。スクロール発生時も close 未操作→自動スクロールの問題回避。
- **タイトル画面**: 起動時に `scr-title` を表示し、🎴 絵文字＋ゲームタイトル＋START ボタン。START で `show("home")` → `.wrap.no-title` クラス付与 → `<h1>`/`.sub` を非表示化してゲーム画面のスペース確保。`show()` は遷移先に応じて `.no-title` を自動ON/OFF(title画面 OFF / 他の画面 ON)。
- セーブデータ互換: v9スキーマ据え置き、UIレイアウトは CSS/DOM のみの変更。

---

## 8. レジェンドパック(希少エンドコンテンツ)

- **入手**: 試合後の確率ドロップのみ(勝利18% / 引分8% / 敗北4%)。コイン購入・通常ガチャ不可。
- **中身**: 1枚。LEGEND 25% / SR 55% / R 20%。
- **目安**: 勝ち continuing で約7〜8試合に1パック、4パックに1枚LEGEND ≒ レジェンド1枚あたり約30勝。
- カードデザイン: 虹色パレットサイクル枠 + 強化ホロ + ✦3粒。

---

## 9. アセット仕様(スプライト)

- 頭部アトラス: 32種、セル `HCW×HCH = 100×96`、8列×4行。
- ボディアトラス: 16種 = ポジション4(`POSCOL={FW:0,MF:1,DF:2,GK:3}`)× ポーズ/バリエーション4、セル `BCW×BCH = 132×88`、4列×4行。
- 合成: ボディを下に描画し、`BODY_ANCHOR[bodyIndex]`(首位置=肌色検出)に頭部を重ねる。
- 縁処理: ソフトアルファ + 背景色除染 + 1px濃紺アウトライン + 孤立/グリッド片除去 + LANCZOS縮小 + WebP(品質88)。表示は `image-rendering:auto`。
- カードごとに `look.headIdx`(0-31)× `look.bodyVar`(0-3)で外見決定。保存される。

---

## 10. カードデザイン(レアリティ別)

| レア | 枠 | 演出 |
|---|---|---|
| N  | 鉄色フラット枠 | なし |
| R  | シルバーメタリック枠+1px銀ライン | 斜め光沢が約5.5秒周期で往復 |
| SR | 金二重枠+金発光 | 虹ホログラム背景(約5秒揺らぎ)+キラ流光+✦粒子2つ明滅 |
| L  | 虹色パレットサイクル枠(6コマ) | 強化ホロ+キラ流光+✦粒子3つ |

- SFCスキン下では SR/L の発光は「枠色の点滅(パレットアニメ風)」で表現。

### 10.1 カードレイアウト / ステータス表示(カード共通)
- カードサイズ **116×190px(mini 96×166px)**。一覧グリッド(`.grid`)は最大520px幅で**横4枚**まで。
- 上部: **ポジション(細分sub)バッジ**(左)。
- 中段(`.radar`): **6ステの六角レーダーを背景**に広げ、中央にスプライトを重ねる(`radarSVG`)。
  - 頂点順(時計回り・上から): OF(攻) / DF(守) / PO(力) / TE(技) / SP(速) / ST(持)。グリッドは3重六角、値ポリゴンは `値/20`。
  - 各頂点に略称+数値(`.rlab`)。**値が20(最大値)の数字は金色+発光(`.mx`)** で強調し、選手の長所を一目で示す。
  - レーダー塗り色はレア度連動(N/R=シアン、SR=金、L=ピンク)。mini では頂点ラベルを非表示にしポリゴン形状のみ表示。
- 下部(`.cinfo`): **選手名 → OVR+レア度 → プレースタイル → スキル名** を縦に配置(いずれも1行・ellipsis)。
- 略称 `STAT_SHORT`(OF/DF/PO/TE/SP/ST)。漢字ラベル(攻/守/…)は試合実況の `STAT_LABEL` で引き続き使用。

---

## 11. テスト / 検証ハーネス(headless Node)

> 試合報酬は **`100+lv×40`(レベル依存)**。勝敗判定は固定値比較せず、必ず最終スコア(home/away)で判定すること(過去に固定140と誤判定したリグレッションあり)。

| スクリプト | 目的 |
|---|---|
| `integration.js` | モックDOMで全4スタイル+交代の試合完走を確認 |
| `hangtest.js` | ストレージ・画像が永久ハングする環境での起動・完走 |
| `scaletest.js` | レア別6ステ合計が目標値(55/70/90/100)に乗るか |
| 進行テスト | デッキを育てながらの想定勝率カーブ(スコア直読み) |
| ID整合チェック | 全 `getElementById` に対応する `id=` がHTMLにあるか |
| マイグレーション検証 | 旧v7(1-9)セーブのv8変換と既存進捗保持 |

**標準リリース手順**: 構文チェック(`node --check`)→ ID整合 → 統合テスト → ハング耐性 → (バランス変更時)進行テスト → 出力。

---

## 12. 決定事項ログ(なぜその数値か)

- **1-20スケール採用**: 長期的な選手追加で 11-20 の広いレンジを使った差別化を可能にするため(1-9では幅不足)。マッチアップが比率ベースなので一律スケールでバランス不変。
- **対戦相手 `avg=6.6+lv×1.0`**: クラブLvが1-8(0始まりではない)。この係数で「デッキを育てれば次ステージに手が届く」滑らかな勝率勾配になる。
- **スタイル補正を圧縮(short 1.15→1.06, shortBonus 1.25→1.12)**: 特定スタイルが強すぎないよう、スタイルを「power pick」ではなく「戦術選択」にするため。
- **レジェンド = 試合ドロップのみ**: "伝説" の希少性を担保(コインで買えると価値が下がる)。
- **ポジション細分化(v9)**: 大分類4種では編成の幅が出ないため、実サッカーの13ポジション(CF/ST/LWG/RWG, OMF/CMF/DMF/LMF/RMF, LSB/CB/RSB, GK)に細分化。ただし大分類はスプライト・プレースタイル・スキルの基準として維持し、細分はあくまで「枠適正(pen)」の解像度を上げる用途に限定(データ・描画資産の作り直しを回避)。
- **2段階適正の数値(1.0/0.92/0.85/0.72)**: 「①大分類の合否」を大きな段差(1.0系↔0.72)で、「②同分類内の細分」を小さな段差(1.0/0.92/0.85)で表現。サイドバック同士(LSB↔RSB)やセンター系MF同士は"ほぼ自然"=0.92、CB→SBのような役割違いは"やや無理"=0.85。完全な専門外(MF→FW)は従来通り0.72を据え置き、リグレッションを避けた。near/far は `SUBNEAR` クラスタで定義(調整時はクラスタ定義を変更)。
- **編成画面のグリッド配置+帯ゾーン(v1.2)**: 横並びの2トップが CF/ST のどちらか分からない等、細分posの判別が困難だった。FORMS座標を実ポジション準拠の標準グリッド(CF最前→ST→OMF→MF段→DMF→DF段→GK)へ並べ替え、中央列を縦に積んで混同を解消。さらに各枠上部に適正ポジション名(slothead)を常時表示し、フィールドを FW/MF/DF/GK の帯ゾーンで色分けして「どの枠がどのポジションか」を一目で分かるようにした。座標は試合エンジンの開始位置も兼ねるが、wide判定(x≤30/≥70)の前提は維持しているため挙動は不変。
- **同国籍ケミストリーは小さく(最大+6%)・対称に(v1.5)**: コレクションを集める動機付けの軽いボーナス。`eff()` の単一集約点に乗算して全判定へ一貫適用。相手チームも同ロジックで算出するため勝率カーブは不変(乱数デッキは3人以上同国籍が稀、検証用クローン対戦では両者相殺)。比率ベースの前提を壊さない範囲に抑えた。国名はflagから導出する方式とし、セーブ互換を維持。
- **ガチャUIをデータ駆動+演出分離に刷新(v1.7)**: 旧UIはカードのリビールが選手/レジェンド2パックの間に挟まり違和感があり、かつパックがハードコードで拡張不可だった。`PACKS` 配列で定義してオーバーレイ演出に統一、リビールを画面中央に独立させて違和感を解消。抽選ロジック `drawPack` をUI(`openPackById`)から分離し、テスト・将来の自動化から再利用できるようにした。パック追加は配列1要素で済む。
- **チャンピオンパック追加(v1.8)**: データ駆動パックの最初の拡張例。リーグ優勝の専用報酬として `S.championPacks` を新設し、優勝報酬を「レジェンドパック1枚」から「チャンピオンパック1枚(5枚入り・SR以上確定)」へ置換(レジェンドパックは試合ドロップに一本化し、報酬の重複を解消)。新フィールドは `||0` ガードで既存セーブ(v9)互換、スキーマ据え置き。`championDraw` の排出はコレクション報酬であり試合バランスには影響しない(強さは試合エンジン側の比率で決まる)。
- **UI全体の使いやすさ改善(v1.9)**: 試合画面のフィールド表示が大きすぎて実況フィード・操作が画面外になる問題を解決。`.fieldview` を `max-height:62vh` → `200px` に縮小してレイアウトをコンパクト化。モーダル(フォーメーション/交代/ピッカー)の高さを `70vh` → `50vh` に制限し、☒ウインドウクローズボタンを絶対位置で各モーダルヘッダに追加（スクロール不要）。**タイトル画面を新設**(`scr-title`)し、起動時に表示。START ボタンで home 画面へ遷移する際に `show()` で `.wrap.no-title` クラスを付与してゲーム中は h1/サブタイトルを非表示化、スペース確保。`show()` は title 画面以外で .no-title を自動ON/OFFする。既存セーブ互換(v9)。
- **相手フォーメーションの個性化＋スタイル相性(v2.0)**: 相手が常に4-4-2固定だと「最強の単一戦略でゴリ押し」できてしまい、相手に応じて対応を変える面白さが無かった。各クラブに得意フォーメーションを与えて実際にその陣形で布陣させ(陣形が画面で見える=相手が別物に感じる)、`FORM_COUNTER` で「守備陣形 × 攻撃スタイル」の有利/不利を定義。**best/worstは机上の理屈でなく試合エンジンの実測**(同強度クローン2880試合の陣形別スタイル得点)に一致させた — そうしないとヒント表示と実挙動が食い違うため(初期の理屈ベース表は3-5-2で「サイド有利」と出したが実測は逆で、修正)。係数は±15%/−12%と控えめにし、勝率を支配せず**チャンス創出という体感**と接戦帯の数%優位に留める(SPECの"スタイル=戦術選択であってpower pickでない"方針を維持)。クラブ配置はside/short/long/centerが各2クラブで答えになり、Lv1〜4で全4スタイルを学べる順序にした。相手フォーメーションと推奨スタイルはステージ選択で事前開示+キックオフでヒント。リーグ戦のCPU即時計算(`simCpu`)は陣形非依存のまま。セーブ互換(v9据え置き、CLUBSは静的データ)。

---

## 13. バックログ(未実装・候補)

- 効果音(Web Audio API のピコピコ音)— 次の有力候補
- (必要に応じてここに追記)
